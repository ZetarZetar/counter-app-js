// Increment counter value
export const incrementCounter = (currentValue) => {
  return currentValue + 1;
};

// Decrement counter value
export const decrementCounter = (currentValue) => {
  return currentValue - 1;
};
